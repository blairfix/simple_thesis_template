
\ProvidesPackage{preamble}


\usepackage{appendix}
\usepackage{array}
\usepackage{adjustbox}
\usepackage{booktabs}
\usepackage{bbding}
\usepackage[font=bf]{caption}
\usepackage{cite}
\usepackage[sectionbib]{chapterbib}
\usepackage{enumitem}
\usepackage{fancyhdr}
\pagestyle{fancy}
\renewcommand{\sectionmark}[1]{\markboth{#1}{}} % set the \leftmark
\fancyhf{}
\fancyhead[R]{\thepage}
\fancyhead[L]{\leftmark} % 1. sectionname
\fancypagestyle{plain}{%
		\fancyhf{}%
		\renewcommand{\headrulewidth}{0pt}%
}
\usepackage{framed}
\usepackage{graphicx}
\usepackage[left=1.5in,right=1.5in,top=1in,bottom=1in, footskip=.5in]{geometry}
\usepackage[colorlinks = true,
linkcolor = blue,
urlcolor  = blue,
citecolor = blue,
anchorcolor = blue]{hyperref}
\urlstyle{same}
\usepackage[utf8x]{inputenc}
\usepackage{mathtools}
\usepackage[bitstream-charter]{mathdesign}
%\usepackage{mathptmx}
\usepackage{multicol}
\usepackage{placeins}
\usepackage{sectsty}
		\chapterfont{\fontsize{18}{18}\selectfont}
		\sectionfont{\fontsize{14}{14}\selectfont}
		\subsectionfont{\fontsize{13}{13}\selectfont}
		\subsubsectionfont{\fontsize{12}{12}\selectfont}
\usepackage[rightcaption]{sidecap}
\sidecaptionvpos{figure}{c}
\usepackage{textcomp}
\usepackage[table]{xcolor}
\usepackage{xr}
\usepackage{tcolorbox}



% left table column with width
\newcolumntype{L}[1]{>{\raggedright\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}

% centered table column with width
\newcolumntype{C}[1]{>{\centering\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}

% erf function
\DeclareMathOperator\erf{erf}

%\DeclareCaptionJustification{nohyphen}{\hyphenpenalty=10000}
\captionsetup{labelfont=bf} 

% Set Parskip
\setlength{\parskip}{4pt}
\renewcommand{\baselinestretch}{1.15}

% Keep footnote on same page
\let\svfootnoterule\footnoterule
\renewcommand\footnoterule{\vfill\svfootnoterule}
\widowpenalty=9999
\setlength{\footnotesep}{\baselineskip}


% Remove newpage from include command
\let\oldInclude=\include
\def\include#1{\bgroup\def\clearpage{\relax}\oldInclude{#1}\egroup}


% Remove brackets from numbering in List of References
\makeatletter
\renewcommand\@biblabel[1]{#1.}
\makeatother

% Rename References
\renewcommand*{\bibname}{References}
%\renewcommand{\chaptername}{}

\bibliographystyle{plos2015}

